<% if @recipient == @quote.quoter %>
<%= link_to_user @comment.user, :actually_link => false, :avatar => false %> just commented on the quote <%= quote_marks_if_needed(@quote.quote_text) %> by <%= link_to_user @quote.quotee, :actually_link => false, :avatar => false %> which you added to <%= @quote.context.name %>:
<% else %>
<%= link_to_user @comment.user, :actually_link => false, :avatar => false %> just commented on your quote <%= quote_marks_if_needed(@quote.quote_text) %> in <%= @quote.context.name %>:
<% end %>
<%= @comment.body %>

You can see this quote and all its comments at <%= quote_url(@quote) %>, and comment on it too if you want to. You can also see all quotes from <%= @quote.context.name %> at <%= context_url(@quote.context) %>.

If you wish to stop receiving these notifications, you can edit your profile to disable email notification by going to <%= edit_user_url(@recipient) %>.
