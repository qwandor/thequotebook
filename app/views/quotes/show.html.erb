<p>
  On <%=h @quote.created_at %>, <%= link_to h(@quote.quoter.username), @quote.quoter %> quoted <%= link_to h(@quote.quotee.fullname), @quote.quotee %> as saying "<%=h @quote.quote_text %>" in <%= link_to h(@quote.context.name), @quote.context %>.
</p>

<%= link_to 'Edit', edit_quote_path(@quote) %> |
<%= link_to 'Destroy', @quote, :confirm => 'Are you sure?', :method => :delete %> |
<%= link_to 'Back', quotes_path %>
